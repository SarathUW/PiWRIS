# Plot function that can handle different types of inputs. 
# So if input is a reservoir object, it can create timeseries plot for data.

# def plot(input_object, **args):
#     if input_object is of class Reservoir
#           return timeseries plot
#     elif isinstance(input_object, GroundWater):
#           return other plot